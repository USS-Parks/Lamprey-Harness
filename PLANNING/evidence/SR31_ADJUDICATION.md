# SR-31 candidate adjudication

Reviewed against SR-30 source `52b4800e1489c73175f46d0a2f954eb17beec1e3`. These are bounded additions to the approved audit, not evidence of completed repairs.

| Candidate | Source evidence and disposition | Follow-up gate |
|---|---|---|
| Historical fork atomicity | `electron/services/fork-task.ts:73-92` creates a worktree and then creates the child outside the cleanup try. Creation failure leaks the worktree; subsequent copied rows are separate transactions. The compensating delete at 139 can itself fail and prevent finalization. Confirmed SA-33. | SR-31A: native SQLite failure injection proves no partial child; worktree creation/finalization failures remain reported and accounted for. |
| Retry cancellation | `providers/registry.ts:1594,1643,1650` sleeps without the parent signal after removing its abort listener. Cancellation can wait the full 2/4/8-second backoff. The next SDK request receives an aborted signal, so this is delayed settlement, not evidence of an unauthorized network request. Confirmed SA-34. | SR-31B: real local HTTP 429/stream failure, cancel during backoff, prompt settlement and no second request. |
| Browser lifecycle | `BrowserPanel.tsx:147-159` has no unmount guard around asynchronous initialization. It can call setVisible(true) after cleanup hid the view. Preload subscriptions use removeAllListeners rather than owned removers. Developer polling can reject unhandled and stale responses can replace the selected tab's status. Confirmed SA-35. | SR-31C: delayed mount/unmount and tab-switch fixture; independent listeners retained; rejected polling handled; real Electron visibility smoke. |
| Quit and subagent drain | `main.ts:723-754` drains loop/wakeup and MCP work only. Regular `turnRuntimeRegistry` entries and `subagent-runner.ts:424` live handles are not enumerated or drained before closeDb. Existing SR-18 proves ordinary idle quit only. Confirmed SA-36. | SR-31D: real Electron active local stream and subagent cancellation, settlement before database close; bounded timeout explicitly reported. |
| Startup ordering | `App.tsx:401-408` releases loading before loadModels/settings complete and does not catch initialization rejection. `model-store.ts:13` starts at DeepSeek. SR-21 acceptance observed an unintended default-provider attempt before the configured fixture model loaded. Model selection also optimistically ignores persistence failure. Confirmed SA-37. | SR-31E: delayed/rejected initialization cannot send on a placeholder model; recoverable error; acknowledged model selection; explicit keyless-local onboarding path. |
| Tool-schema supported subset | `tool-schema-validator.ts:112-138` does not enforce parsed object roots; 208-210 accepts null for all types; integer accepts fractions; nested required and array item recursion are incomplete. These contradict its documented supported subset. Confirmed SA-38. | SR-31F: recursive supported-subset tests and dispatch fixture prove invalid calls never reach a handler; preserve explicitly documented no-argument compatibility. |
| Workflow status navigation | `StatusLine.tsx:259-262` emits workflows:openPalette with no listener anywhere in src. Confirmed SA-39. | SR-31G: reuse existing UI-store palette action; click real rendered status affordance and verify palette opens. |
| Dependency advisories | Current npm audit returns 27 affected package entries: 1 critical, 17 high, 7 moderate, 2 low. `sr31-advisories.json` records package paths, inherited findings, advisory URLs and available fixes. This is dependency evidence, not a claim that every advisory is exploitable in the packaged app. SA-40 requires remediation/reachability review. | SR-31H: review runtime/build reachability, update compatible affected packages, verify lock/native/build behavior and rescan; explicitly document any unresolved advisory without calling it fixed. |
| Review untracked read | `electron/ipc/review.ts` executes an unused ls-files call before the actual file read. No correctness defect established beyond redundant work. | SR-31I: remove only the unused call after checking its branch, preserve file boundary behavior and run affected checks. |
| Environment refresh rejection | `useEnvironment` callers include mount/poll refresh without rejection handling. Shared Git actions now catch refresh failure, but autonomous refresh still needs a handled failure state. | SR-31I: reject mount/poll refresh in a component fixture and verify recovery and cleanup. |

The existing R1-R4/provider-capability live playbooks remain parked under their earlier explicit dispositions. They do not replace any of these acceptance gates. The intermittent Windows CI failures remain an open verification issue for SR-34; a rerun is not a demonstrated product repair.

SR-31D live follow-up: quitting at a pending tool approval initially exhausted the grace period because aborting the turn did not resolve the approval promise. The same shutdown repair now denies pending approvals and cancels pending ask-user requests before draining. Real Electron verified both pending approval (no child request) and allowed local child (cancelled parent, aborted child), inspecting SQLite before startup recovery could alter it.

SR-31G follow-up evidence: `setWorkflowChatRunner` has no production caller (`rg` finds only its definition and explanatory strings in `electron/ipc/workflows.ts`). The existing workflow IPC therefore cannot execute from the palette. Expand SR-31G's existing navigation repair to wire the existing provider runner and prove a local fixture workflow executes; do not claim success from palette opening alone.

Authored and reviewed by Basho Parks, copyright 2026
