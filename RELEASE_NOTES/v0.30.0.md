# Lamprey v0.30.0 — Operability Debt

Released 2026-08-23.

## Settlement

- Cap failure, cancel, and the closer now have behavioral tests that call
  `finalizeTurn` / `interruptTurn`, not only WC-8 source-reading.
- A tool-round cap still settles `failed`. Cancel still settles `cancelled`.
- The audit event remains `turn.interrupted` with payload
  `disposition: 'interrupted'`. That pair is locked.

## Honesty

- R1–R4 live playbooks, live `supportsTools` probes, and OpenWiki refresh
  are **parked**. Unsigned builds are a **permanent non-goal**.
- Mid-round document/artifact drains stay on the persist path. Comments
  say they are not the closer. `finalizeTurn` still owns close-drain.

## Catalog and persist

- The twelve August 2026 first-party rows live in
  `electron/services/providers/catalog.ts`. The overlay file and
  import-time `push` loop are gone.
- `tool_search` unlock persist rethrows unexpected SQLite errors.
  A missing table still no-ops.

## Planning

- Spent P-SPRs moved to `PLANNING/archive/`. Audit Closure authority
  files stayed at the root.

Authored and reviewed by Basho Parks, copyright 2026
