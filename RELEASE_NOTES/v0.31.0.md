# Lamprey v0.31.0 — Triple Lane

Released 2026-08-23. Source wrap. Installer artifacts stay on v0.30.0
until Bucket (TL-W4, coordinator).

## Lane A — Claude Code refresh

- Inventory pinned to Claude Code **2.1.241** (GitHub tag 2026-08-23).
- Pick list empty (K12). Empty Lane A is success. No product deltas.

## Lane B — OpenRouter routing depth

- Opt-in OpenRouter path can send fallbacks plus provider
  sort / order / ignore. Empty routing stays `{}` (K4).
- `openrouter/auto` is offered when an OpenRouter key is present. It is
  not a `MODEL_CATALOG` row.
- Direct-provider default is unchanged. Catalog still has zero
  OpenRouter-brokered aliases.

## Lane C — Local endpoint polish

- Settings presets for Ollama, LM Studio, and Unsloth Studio API.
- Connection-refused probes use loud copy instead of a generic fetch
  failure.
- Live local imports still default tools and vision off. Local tools
  still pass approval / snip / gates (K10).
- No Unsloth training UI in core (K6).

## Non-goals held

- No Unburdening rebuild.
- No `MODEL_CATALOG` fabrication.
- No plugin-tool runtime.
- No Bucket on this wrap.

Authored and reviewed by Basho Parks, copyright 2026
